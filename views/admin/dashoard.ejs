<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management</title>
    <link rel="stylesheet" href="/css/admin-users.css">
    <meta name="description" content="User management dashboard for admin system">
</head>
<body>
    <div class="admin-container">
        <header class="admin-header">
            <h1><i class="fas fa-users-cog"></i> User Management</h1>
            <div class="admin-info">
                <span>Welcome, <%= typeof user !== 'undefined' ? user.username : 'Admin' %></span>
                <a href="/admin/logout" class="logout-btn" title="Logout">
                    <i class="fas fa-sign-out-alt"></i>
                </a>
            </div>
        </header>

        <main class="admin-main">
            <div class="quick-actions">
                <a href="/admin/user/add" class="action-btn" id="add-user-btn">
                    <i class="fas fa-user-plus"></i> Add User
                </a>
                <a href="/admin/user/edit" class="action-btn" id="edit-user-btn">
                    <i class="fas fa-user-edit"></i> Edit Account
                </a>
                <a href="/admin/user/charge" class="action-btn" id="charge-balance-btn">
                    <i class="fas fa-coins"></i> Charge Balance
                </a>
            </div>
        </main>
    </div>

    <script src="/js/admin-users.js"></script>
</body>
</html>